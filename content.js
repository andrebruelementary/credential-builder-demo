console.log("content script running!!")

/*chrome.runtime.onMessage.addListener(gotMessage);
function gotMessage(message, sender, sendResponse) {
  console.log(message.txt);
  if(message.txt === "Hello") {
    
  }
}*/


//document.getElementById('new_did_button').addEventListener("click", newDIDButtonClicked)

//function newDIDButtonClicked() {
//    console.log('new did button clicked')
    
    /*
          function getURL() {
            chrome.runtime.sendMessage({greeting: "GetURL"},
            function (response) {
                back = response.responseBack;
                alert("response received "+ back);
            });
          }
      */
//}